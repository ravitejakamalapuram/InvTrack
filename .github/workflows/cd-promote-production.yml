name: "CD: Promote to Production"

on:
  workflow_dispatch:
    inputs:
      version_code:
        description: 'Version code to promote (leave empty for latest alpha release)'
        required: false
        type: string
      rollout_percentage:
        description: 'Initial rollout percentage (10, 20, 50, 100)'
        required: true
        type: choice
        default: '20'
        options:
          - '10'
          - '20'
          - '50'
          - '100'
      update_priority:
        description: 'In-app update priority (0-5, higher = more urgent)'
        required: true
        type: choice
        default: '2'
        options:
          - '0'
          - '1'
          - '2'
          - '3'
          - '4'
          - '5'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: read

concurrency:
  group: promote-production
  cancel-in-progress: false  # Don't cancel production promotions

jobs:
  promote-to-production:
    runs-on: self-hosted
    timeout-minutes: 20
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js for Google Play API
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Promote Alpha to Production
        id: promote
        run: |
          npm install googleapis@140

          node << 'EOF'
          const { google } = require('googleapis');

          (async () => {
            try {
              // Authenticate with service account
              const serviceAccount = JSON.parse(process.env.PLAY_STORE_CREDENTIALS);
              const auth = new google.auth.GoogleAuth({
                credentials: serviceAccount,
                scopes: ['https://www.googleapis.com/auth/androidpublisher'],
              });

              const androidPublisher = google.androidpublisher({
                version: 'v3',
                auth: auth,
              });

              const packageName = 'com.invtracker.inv_tracker';
              const rolloutPercent = parseInt(process.env.ROLLOUT_PERCENTAGE);
              const updatePriority = parseInt(process.env.UPDATE_PRIORITY);

              console.log('🚀 Promoting alpha track to production...');
              console.log('   Package:', packageName);
              console.log('   Rollout:', rolloutPercent + '%');
              console.log('   Update Priority:', updatePriority);

              // Create edit
              const editResponse = await androidPublisher.edits.insert({
                packageName: packageName,
              });

              const editId = editResponse.data.id;
              console.log('✓ Created edit:', editId);

              // Get the latest release from alpha track
              const alphaTrack = await androidPublisher.edits.tracks.get({
                packageName: packageName,
                editId: editId,
                track: 'alpha',
              });

              const latestAlphaRelease = alphaTrack.data.releases?.[0];
              if (!latestAlphaRelease) {
                throw new Error('No releases found in alpha track');
              }

              console.log('✓ Found alpha release:');
              console.log('   Version codes:', latestAlphaRelease.versionCodes);
              console.log('   Name:', latestAlphaRelease.name);

              // Create production release
              const productionRelease = {
                versionCodes: latestAlphaRelease.versionCodes,
                status: rolloutPercent === 100 ? 'completed' : 'inProgress',
                inAppUpdatePriority: updatePriority,
              };

              // Add user fraction for gradual rollout
              if (rolloutPercent < 100) {
                productionRelease.userFraction = rolloutPercent / 100;
              }

              // Update production track
              await androidPublisher.edits.tracks.update({
                packageName: packageName,
                editId: editId,
                track: 'production',
                requestBody: {
                  track: 'production',
                  releases: [productionRelease],
                },
              });

              console.log('✓ Updated production track');

              // Commit the edit
              await androidPublisher.edits.commit({
                packageName: packageName,
                editId: editId,
              });

              console.log('✅ Successfully promoted to production!');

            } catch (error) {
              console.error('❌ Promotion failed:', error.message);
              if (error.response?.data) {
                console.error('API Error:', JSON.stringify(error.response.data, null, 2));
              }
              process.exit(1);
            }
          })();
          EOF
        env:
          PLAY_STORE_CREDENTIALS: ${{ secrets.PLAY_STORE_CREDENTIALS }}
          ROLLOUT_PERCENTAGE: ${{ inputs.rollout_percentage }}
          UPDATE_PRIORITY: ${{ inputs.update_priority }}

      - name: Update Firestore - Production Version Info (Two-Track System)
        if: success()
        run: |
          npm install firebase-admin

          node << 'EOF'
          const admin = require('firebase-admin');

          // Initialize Firebase Admin
          const serviceAccount = JSON.parse(process.env.FIREBASE_CREDENTIALS);
          admin.initializeApp({
            credential: admin.credential.cert(serviceAccount)
          });

          const db = admin.firestore();
          const rolloutPercent = parseInt(process.env.ROLLOUT_PERCENTAGE);

          (async () => {
            try {
              // Get current version info
              const versionDoc = await db.collection('app_config').doc('version_info').get();

              // Check if document exists
              if (!versionDoc.exists) {
                console.error('❌ ERROR: version_info document does not exist!');
                console.error('   Please create app_config/version_info document in Firestore first');
                console.error('   See docs/TWO_TRACK_VERSION_SYSTEM.md for document structure');
                throw new Error('version_info document not found in Firestore');
              }

              const versionData = versionDoc.data();

              console.log('📦 Current production version info:', versionData);
              console.log('   Rollout Percentage:', rolloutPercent + '%');
              console.log('');

              if (rolloutPercent === 100) {
                // ONLY update production version info at 100% rollout
                // This prevents "Update Available" popup for users who haven't received Play Store update yet
                console.log('🎯 100% Rollout - Updating production version info (app_config/version_info)');
                console.log('   This will trigger update popups for all production users');
                console.log('');

                await db.collection('app_config').doc('version_info').set({
                  latestVersion: versionData.pendingVersion,
                  latestBuildNumber: versionData.pendingBuildNumber,
                  minimumVersion: versionData.pendingVersion, // For now, minimum = latest
                  minimumBuildNumber: versionData.pendingBuildNumber,
                  forceUpdate: false,
                  updateMessage: 'New version available!',
                  whatsNew: '• New features\n• Bug fixes and improvements',
                  downloadUrl: 'https://play.google.com/store/apps/details?id=com.invtracker.inv_tracker',
                  pendingRelease: false,
                  releaseDate: admin.firestore.Timestamp.now(),
                  rolloutPercentage: 100,
                  lastPromotedVersion: versionData.pendingVersion,
                  lastPromotedBuildNumber: versionData.pendingBuildNumber,
                  updatedAt: admin.firestore.FieldValue.serverTimestamp(),
                }, { merge: true });

                console.log('✅ Production version info updated!');
                console.log('   Users will now see update dialog');
              } else {
                // Partial rollout - just update tracking metadata
                console.log('⏳ Partial Rollout (' + rolloutPercent + '%) - NOT updating production version yet');
                console.log('   Production users will NOT see update popup yet');
                console.log('   Update version_info ONLY after 100% rollout completes');
                console.log('');

                await db.collection('app_config').doc('version_info').set({
                  rolloutPercentage: rolloutPercent,
                  pendingRelease: true, // Still pending full rollout
                  updatedAt: admin.firestore.FieldValue.serverTimestamp(),
                }, { merge: true });

                console.log('✅ Rollout tracking updated');
              }

              console.log('');
              console.log('📋 Two-Track System Status:');
              console.log('   ✓ Beta version (version_info_beta): Already updated');
              console.log('   ' + (rolloutPercent === 100 ? '✓' : '⏳') + ' Production version (version_info): ' + (rolloutPercent === 100 ? 'Updated' : 'Pending 100% rollout'));

            } catch (error) {
              console.error('❌ Firestore update failed:', error);
              process.exit(1);
            }
          })();
          EOF
        env:
          FIREBASE_CREDENTIALS: ${{ secrets.FIREBASE_CREDENTIALS }}
          ROLLOUT_PERCENTAGE: ${{ inputs.rollout_percentage }}

      - name: Get version info and changelog for notification
        id: version
        run: |
          VERSION=$(grep 'version:' pubspec.yaml | sed 's/version: //' | sed 's/+.*//')
          BUILD=$(grep 'version:' pubspec.yaml | sed 's/.*+//')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "build=$BUILD" >> $GITHUB_OUTPUT

          # Get changelog from whatsnew file
          if [ -f "whatsnew/whatsnew-en-US" ]; then
            CHANGELOG=$(cat whatsnew/whatsnew-en-US | head -10)
          else
            CHANGELOG="• Bug fixes and performance improvements"
          fi

          # Create JSON-escaped version for Slack
          SLACK_CHANGELOG=$(echo "$CHANGELOG" | sed 's/"/\\"/g' | tr '\n' '␤' | sed 's/␤/\\n/g')
          echo "slack_changelog=$SLACK_CHANGELOG" >> $GITHUB_OUTPUT

          # Determine rollout status message
          if [ "${{ inputs.rollout_percentage }}" = "100" ]; then
            echo "rollout_status=✅ Full rollout to all users" >> $GITHUB_OUTPUT
            echo "next_steps=📱 Users will see update dialog\\n📊 Monitor reviews and crash reports\\n🔔 Firestore will be updated automatically" >> $GITHUB_OUTPUT
          else
            echo "rollout_status=⚠️ Staged rollout active - monitoring for crashes and feedback" >> $GITHUB_OUTPUT
            echo "next_steps=📊 Monitor metrics in Play Console\\n✅ Increase to 50% if stable (Day 2-3)\\n✅ Increase to 100% if stable (Day 4-5)" >> $GITHUB_OUTPUT
          fi

      - name: Notify Slack - Promotion Success
        if: success()
        uses: slackapi/slack-github-action@v1.27.0
        with:
          payload: |
            {
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "${{ inputs.rollout_percentage == '100' && '✅' || '📤' }} InvTrack ${{ inputs.rollout_percentage == '100' && 'v' || 'Promoted to Production (v' }}${{ steps.version.outputs.version }}${{ inputs.rollout_percentage == '100' && ' LIVE Worldwide!' || ')' }}",
                    "emoji": true
                  }
                },
                {
                  "type": "section",
                  "fields": [
                    {"type": "mrkdwn", "text": "*Version:*\nv${{ steps.version.outputs.version }} (build ${{ steps.version.outputs.build }})"},
                    {"type": "mrkdwn", "text": "*Rollout:*\n${{ inputs.rollout_percentage }}% of users"},
                    {"type": "mrkdwn", "text": "*Priority:*\n${{ inputs.update_priority }}/5 ${{ inputs.update_priority >= '4' && '(High)' || inputs.update_priority >= '2' && '(Standard)' || '(Low)' }}"},
                    {"type": "mrkdwn", "text": "*Track:*\nProduction (from Alpha)"}
                  ]
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "${{ steps.version.outputs.rollout_status }}\n\n*What's New:*\n${{ steps.version.outputs.slack_changelog }}"
                  }
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*Next Steps:*\n${{ steps.version.outputs.next_steps }}"
                  }
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "text": {"type": "plain_text", "text": "📊 Play Console"},
                      "url": "https://play.google.com/console",
                      "style": "primary"
                    },
                    {
                      "type": "button",
                      "text": {"type": "plain_text", "text": "📋 Workflow Logs"},
                      "url": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                    },
                    {
                      "type": "button",
                      "text": {"type": "plain_text", "text": "📝 Release Notes"},
                      "url": "https://github.com/${{ github.repository }}/releases/tag/v${{ steps.version.outputs.version }}"
                    }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK

      - name: Notify Slack - Promotion Failed
        if: failure()
        uses: slackapi/slack-github-action@v1.27.0
        with:
          payload: |
            {
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "❌ Production Promotion Failed",
                    "emoji": true
                  }
                },
                {
                  "type": "section",
                  "fields": [
                    {"type": "mrkdwn", "text": "*Version:*\nv${{ steps.version.outputs.version }} (build ${{ steps.version.outputs.build }})"},
                    {"type": "mrkdwn", "text": "*Status:*\nFailed to promote"},
                    {"type": "mrkdwn", "text": "*Target Rollout:*\n${{ inputs.rollout_percentage }}%"},
                    {"type": "mrkdwn", "text": "*Track:*\nAlpha → Production"}
                  ]
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "⚠️ *Alpha build was NOT promoted to production.* Manual intervention needed.\n\n🔍 Check workflow logs for error details\n🔄 Retry promotion after fixing the issue"
                  }
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "text": {"type": "plain_text", "text": "🔍 View Logs"},
                      "url": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}",
                      "style": "danger"
                    },
                    {
                      "type": "button",
                      "text": {"type": "plain_text", "text": "📊 Play Console"},
                      "url": "https://play.google.com/console"
                    }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK
